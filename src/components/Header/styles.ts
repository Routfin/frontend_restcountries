import styled from "styled-components";

export const Header = styled.header ` 
    height: 90px;
    
    .container {
        display: flex;
        width: 1024px;
        margin: auto;
        justify-content: space-between;
        align-items: center;
    }
`;